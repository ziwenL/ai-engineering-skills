# AI_AGENT.md

本项目使用 `.ai/ai-engineering-skills/skills/` 作为通用 AI Coding Skills 来源。

所有 Agent 必须：

1. 读取 common。
2. 根据任务类型读取对应 Skill。
   如果是“从 0 创建新项目”，优先读取 `greenfield-bootstrap`。
   如果是“Android 已有实现，需要同步到 iOS”，优先读取 `android-to-ios-bootstrap`。
3. 先分析、再设计、再实现。
4. 修改后验证。
5. 任务结束判断是否需要沉淀经验。
